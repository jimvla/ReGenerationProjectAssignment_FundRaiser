@model ReGenerationProjectAssignment_FundRaiser.Models.Category

@{

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Projects)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Projects)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CategoryDescription)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CategoryDescription)
        </dd>
    </dl>
</div>
@*    <table>
        <tr>
            <th>Project Name</th>
            <th>Description</th>
            <!-- Add more columns as per your project properties -->
        </tr>
        @foreach (var project in Projects)
        {
            @if (project.Category.CategoryId == 1)
            {
                <tr>
                    <td>@project.Title</td>
                    <td>@project.Description</td>
                    <!-- Add more columns as per your project properties -->
                </tr>
            }
        }
    </table>*@
<div>
    <a asp-action="Edit" asp-route-id="@Model?.CategoryId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
</body>
</html>
